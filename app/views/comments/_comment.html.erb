<div class="comment">
  <h3>
    <%= comment.name %> (<%= comment.email %>) said:
    <span class="actions">
      <%= link_to 'Delete', article_comment_path(article_id: @article, id: comment), method: :delete, data: { confirm: 'Are you sure?' } %>
    </span>
  </h3>
  <%= comment.body %>
</div>